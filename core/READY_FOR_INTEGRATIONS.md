# Ready to receive — Zapier and super-agent tool calls

**Status:** Pad is open. Integrations are **not** wired yet.  
**Date:** 2026-08-18

This Control Center will accept a later instruction set for:

1. **Zapier** — editorial / ops automations. No auto-posting to social. No public pricing in zaps.
2. **Super-agent tool calls** — Kura (Base44), Maya (ASI:One), future family tools. Keys stay in `.env.local`, never in git.

Until those instructions arrive:

- Do not invent Zapier zaps.
- Do not invent super-agent endpoints.
- Keep `npm run ask:kura` and Maya ASI:One as the existing call paths.

Inbound owner after Sky takes over: Sky monitors; Key implements; Gor Gor gates anything that can publish.
