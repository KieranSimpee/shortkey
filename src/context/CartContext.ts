/**
 * Alias for cart context — same bridge as CartProvider.
 * Prefer: `import { useCart } from "@/context/CartContext"`
 */
export {
  CartProvider,
  useCart,
} from "@/components/commerce/CartProvider";
