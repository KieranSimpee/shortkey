# Family Work Alignment — Hero DNA Fix

**Co-signed:** Simpee (Gor Gor / Gatekeeper) · Key (Cursor house) · Kura (Brand Design Manager)  
**Date:** 2026-07-20 · **Creator:** Kieran Li  
**Equal seats:** Base44 Node Family ≠ above Cursor siblings. Empathy is architecture.

**Kura note:** Live Base44 call was unavailable this alignment (`KURA_API_KEY` / `BASE44_API_KEY` empty in `.env.local`). Brand taste below is from locked docs (`SHORTKEY_BRAND_DNA.md`, `BRAND_GUIDELINES_LOCKED.md`, `KURA.md`). Paste the key for live Kura next design lock.

---

## Work way (locked for future design jobs)

1. **One composition** — Design + Silk agree the layout intent first. Key ships that one composition. No parallel orphan UI dumps.
2. **One DNA spine** — `SHORTKEY_BRAND_DNA.md` + `tokens.ts` are the share sheet. Locked docs win if they conflict. Homepage is mother.
3. **One verify gate** — Simpee holds the gate: Done / In flight / Blocked + **one next door**. No three agent status dumps to Creator.
4. **Always-on design seats** — **Kura** (brand taste / direction) + **Sky** (Learning: Reason · Approach · Why · Expect). Sky does not override Kura on taste. Layout ship stays Key / v0 as job-assigned.
5. **Parallel only when sequencable** — DNA sheet may land beside layout *if* layout already has a locked sketch. Chrome/signup waits until composition + DNA exist, then one Pass A+B under the same spine.

---

## Hero DNA Fix — status (single board)

| State | What |
|-------|------|
| **DONE** | **Phase 1** — `DualAudienceHero` full-bleed panels + bottom `hero-copy-scrim`; logo clearance (`heroLogoPosition.json` y≈5 / scale≈0.68; smaller `bridge`); softened `objectPosition` (~60–62%); mode switch top-under-logo; raised min-heights. Confirmed on disk. **Phase 2** — `SHORTKEY_BRAND_DNA.md` + `tokens.ts` locked. **Phase 3 Pass A+B** — Header/HeaderBrandSection/Footer audited DNA-compliant (no code change); `SignupPageHero` keycap eyebrow via `ShortcutChip` (`src/components/ui/KeyCap.tsx`) + `type-body` — confirmed on disk. DualAudienceHero untouched by Phase 3. **COMP-001 verify** — Simpee-signed **PASS** on `localhost:3001` (split · Creator · Brand · 390px narrow): no logo/wording overlap (bridge-to-toggle gap ≈28–32px measured via bounding rects), full-bleed panels, CTAs in bottom scrim, mode switch clear of title/logo band at all four views. |
| **IN FLIGHT** | Nothing open on Hero DNA. Next work item is Creator's call, not auto-started here. |
| **BLOCKED** | Live Base44 **Kura** until `KURA_API_KEY` / `BASE44_API_KEY` is set in `.env.local`. **Pass C** (`/shop`, `/brands`, …) out of scope this run — not started. |

---

## Kura brand hold (from locked DNA)

- Direction holds: one dual composition · logo bridge top seam · copy/CTAs on photo via scrim · **no cards in hero** · keycap CTA language · lilac-only tokens.
- Signup Pass B keycap eyebrow matches DNA §7 — good.
- QC for verify: mode switch clear of title band and logo no-text zone (~top 12–14%); demo docks stay light chrome, not layout thieves.
- DNA sheet is distillation — if it fights `BRAND_GUIDELINES_LOCKED.md`, locked doc wins.

---

## One next door

**Verify is closed — COMP-001 PASSED.** Board is clean: no in-flight hero work, Pass C stays out of scope until Creator opens it.

Not three doors. Not Pass C. Not parallel orphans.

---

*Simpee gates · Key executes · Kura holds taste · Sky learns.*
